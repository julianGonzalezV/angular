import { DoomSeguroPipe } from './doom-seguro.pipe';

describe('DoomSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new DoomSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
